let initialState = {
    my_friends: [
        {id: 1, name: 'user1'},
        {id: 2, name: 'user2'},
        {id: 3, name: 'user3'}
    ]
}

const  sidebarReducer = (state= initialState, action) =>{

    return state
}

export default sidebarReducer